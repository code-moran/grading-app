# Database Configuration
# For Vercel Postgres: Use POSTGRES_PRISMA_URL from Vercel dashboard
# For external databases: Use full connection string with SSL if required
DATABASE_URL=postgresql://username:password@localhost:5432/grading_app?schema=public

# Optional: Separate database configuration (if not using DATABASE_URL)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=grading_app
DB_USER=username
DB_PASSWORD=password

# NextAuth Configuration
# Generate a secure secret: openssl rand -base64 32
# For production: Use your Vercel deployment URL (e.g., https://your-app.vercel.app)
NEXTAUTH_SECRET=your-secret-key-here-change-in-production
NEXTAUTH_URL=http://localhost:3000

# Database Initialization (optional)
# Used for database initialization API endpoint
INIT_DB_SECRET=init-secret-change-in-production

# Environment
# Set to 'production' for production deployments
NODE_ENV=development
